services:
  mailisearch:
    image: getmeili/meilisearch:latest
    container_name: mailisearch
    ports:
      - "7700:7700"
    volumes:
      - mailisearch_data:/data.ms
    environment:
      - MEILI_MASTER_KEY=your_master_key_here
    restart: unless-stopped

volumes:
  mailisearch_data:
